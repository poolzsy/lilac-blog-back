<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lilac.mapper.MenuMapper">
    <select id="selectPermsByUserId" resultType="java.lang.String">
        SELECT
            DISTINCT m.perms
        FROM
            `user_role` ur
                LEFT JOIN `role_menu` rm ON ur.`role_id` = rm.`role_id`
                LEFT JOIN `menu` m ON m.`id` = rm.`menu_id`
        WHERE
            ur.`user_id` = #{userId} AND
            m.`type` IN ('C','F') AND
            m.`status` = 0 AND
            m.`del_flag` = 0
    </select>

    <select id="selectAllRouterMenu" resultType="com.lilac.pojo.entity.Menu">
        SELECT
            DISTINCT m.id, m.parent_id, m.menu_name, m.path, m.component, m.status, IFNULL(m.perms,'') AS perms,
                     m.is_frame, m.type, m.icon, m.sort, m.create_time
        FROM
            `menu` m
        WHERE
            m.`type` IN ('C','M') AND
            m.`status` = 0 AND
            m.`del_flag` = 0
        ORDER BY
            m.parent_id,m.sort
    </select>

    <select id="selectRouterMenuTreeByUserId" resultType="com.lilac.pojo.entity.Menu">
        SELECT
            DISTINCT m.id, m.parent_id, m.menu_name, m.path, m.component, m.status, IFNULL(m.perms,'') AS perms,
                    m.is_frame, m.type, m.icon, m.sort, m.create_time
        FROM
            `user_role` ur
                LEFT JOIN `role_menu` rm ON ur.`role_id` = rm.`role_id`
                LEFT JOIN `menu` m ON m.`id` = rm.`menu_id`
        WHERE
            ur.`user_id` = #{userId} AND
            m.`type` IN ('C','M') AND
            m.`status` = 0 AND
            m.`del_flag` = 0
        ORDER BY
            m.parent_id,m.sort
    </select>
</mapper>